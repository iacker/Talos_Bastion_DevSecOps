# Gitea configuration
# Chart: gitea/gitea v12.5.0

gitea:
  admin:
    username: gitea_admin
    email: admin@local.dev
    # password: <SET VIA --set gitea.admin.password=XXX>

service:
  http:
    type: ClusterIP
  ssh:
    type: ClusterIP

persistence:
  size: 10Gi
