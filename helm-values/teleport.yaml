# Teleport configuration
# Chart: teleport/teleport-cluster v18.6.6

chartMode: standalone
clusterName: teleport.talos.local

proxyListenerMode: multiplex

service:
  type: ClusterIP

persistence:
  enabled: true
  size: 5Gi
